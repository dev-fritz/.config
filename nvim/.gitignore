# Spell dictionaries downloaded on demand
spell/

# Generated by lua_ls / lazydev when it indexes the project
.luarc.json

# Local files not worth versioning
*.log
tags

# Note: lazy-lock.json is deliberately NOT ignored. It pins the exact version of
# every plugin and is what makes this config behave the same on any machine.
# Always commit its changes.
